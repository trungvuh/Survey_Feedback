const authReducer = (state = {}, action) => {
  Object.freeze(state);

  switch (action.type) {
    // case value:

    //   break;

    default:
      return state;
  }
};

export default authReducer;
